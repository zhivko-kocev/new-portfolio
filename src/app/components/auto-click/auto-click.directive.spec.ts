import { AutoClickDirective } from '../auto-click/auto-click.directive';

describe('AutoClickDirective', () => {
  it('should create an instance', () => {
    // const directive = new AutoClickDirective();
    expect(directive).toBeTruthy();
  });
});
