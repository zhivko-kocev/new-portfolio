import { ClickStyleDirective } from './click-style.directive';

describe('ClickStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickStyleDirective();
    expect(directive).toBeTruthy();
  });
});
